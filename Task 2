with open("word.txt", "r", encoding='utf-8') as f:
    content = f.readlines()
    for num, _ in enumerate(content, 1):
        print(f"String - {num}, word- {len(_.split())}")
print(f"Total strings - {len(content)}")
